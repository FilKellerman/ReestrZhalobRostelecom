<Window x:Class="ReestrObrashcheniy.AddEditОтветWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить / Редактировать ответ" 
        Height="420" Width="580"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize"
        Background="#F5F7FA"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Фирменные цвета Ростелекома -->
        <SolidColorBrush x:Key="RostelecomBlue" Color="#0055A5"/>
        <SolidColorBrush x:Key="RostelecomBlueHover" Color="#0066C2"/>
        <SolidColorBrush x:Key="RostelecomGray" Color="#6C757D"/>
        <SolidColorBrush x:Key="GrayDark" Color="#333333"/>
        <SolidColorBrush x:Key="GrayLight" Color="#D0D7E0"/>

        <!-- Стиль TextBox -->
        <Style x:Key="RtlTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="{StaticResource GrayLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource RostelecomBlue}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль CheckBox -->
        <Style x:Key="RtlCheckBox" TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource GrayDark}"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <!-- Стиль кнопки -->
        <Style x:Key="RtlButton" TargetType="Button">
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="MinWidth" Value="110"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Основная кнопка "Сохранить" -->
        <Style x:Key="RtlPrimaryButton" BasedOn="{StaticResource RtlButton}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource RostelecomBlue}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource RostelecomBlueHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#004488"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Вторичная кнопка "Отмена" -->
        <Style x:Key="RtlSecondaryButton" BasedOn="{StaticResource RtlButton}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource RostelecomGray}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5A6268"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль TextBlock -->
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource GrayDark}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Заголовок -->
        <Border Background="{StaticResource RostelecomBlue}" Height="50" VerticalAlignment="Top">
            <TextBlock Text="Добавить / Редактировать ответ"
                       Foreground="White"
                       FontSize="18"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Основное содержимое -->
        <Grid Margin="20,60,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Обращение ID:"/>
            <TextBlock x:Name="txtОбращениеID" Grid.Row="1" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>

            <TextBlock Grid.Row="2" Text="Текст ответа:"/>
            <TextBox x:Name="txtТекст" Grid.Row="3" Style="{StaticResource RtlTextBox}" AcceptsReturn="True" TextWrapping="Wrap" Height="110"/>

            <CheckBox x:Name="chkВнутренний" Grid.Row="4" Style="{StaticResource RtlCheckBox}" 
                      Content="Внутренний комментарий (не виден клиенту)"/>

            <!-- Кнопки -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Отмена" Style="{StaticResource RtlSecondaryButton}" Click="BtnCancel_Click"/>
                <Button Content="Сохранить" Style="{StaticResource RtlPrimaryButton}" Margin="15,0,0,0" Click="BtnSave_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>